<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.myapplication">

    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.ACCESS_WIFI_STATE" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/app_logo"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.MyApplication">
        <activity android:name=".bottomnavigation.ui.home.MySearchResultActivity" />
        <activity android:name=".bottomnavigation.ui.home.IdleGoodsDetailInfoActivity" />
        <activity
            android:name=".bottomnavigation.ui.my.MyCollectedListActivity"
            android:label="收藏夹" />
        <activity
            android:name=".bottomnavigation.ui.my.MyDetailInfoActivity"
            android:label="个人详情" />
        <activity
            android:name=".register.RegisterActivity"
            android:label="@string/title_activity_register" />
        <activity
            android:name=".login.LoginActivity"
            android:label="@string/title_activity_login" /> <!-- 设置进入该页面时从返回栈中查找而不是重新创建 -->
        <activity
            android:name=".bottomnavigation.BottomNavigationActivity"
            android:launchMode="singleTask">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>